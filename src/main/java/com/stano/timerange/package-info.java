/**
 * An immutable time-of-day range type.
 *
 * <p>{@link com.stano.timerange.TimeRange} represents an inclusive range of times within a day,
 * with overlap logic that correctly handles ranges ending at midnight.
 */
package com.stano.timerange;
